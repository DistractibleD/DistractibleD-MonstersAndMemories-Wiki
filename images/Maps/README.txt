Full-size map images go in this folder, named to match the "image" path
listed for that map in maps.json (at the root of the project).

To add a new map to the Maps page, add an entry to maps.json and drop
the map image here with a matching filename. No other files need to
change — the Maps page picks up new entries automatically and lists
them alphabetically.
