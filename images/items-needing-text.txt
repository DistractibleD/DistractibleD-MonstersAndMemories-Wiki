Items with possibly missing/cut-off text
=========================================

I reviewed all 34 item screenshots currently in images/items/ looking for
signs that the in-game popup's text got cut off by the screenshot (missing
bottom border, text running to the raw image edge, mid-word/mid-sentence
cutoffs, etc).

Result: none found. Every screenshot shows a complete popup card with its
decorative border fully visible top and bottom, so nothing needs manual
text added right now.

Update (2026-07-02, inbox batch of 22 screenshots):

- Torch (images/items/torch.png) — the bottom decorative border isn't
  visible in this screenshot; "Race: ALL" runs right to the raw image
  edge. The card also has an "Effect:" line ("Torch (Passive. Must
  Equip. Level: 1)") that may have more detail below what's shown. If
  there's additional text below "Race: ALL" (or the Effect line was
  longer), please add it here.

Everything else in that batch (20 new items) showed a complete card.

This file will be updated the next time new items are processed from the
inbox, in case a future screenshot does turn out to be cut off.
